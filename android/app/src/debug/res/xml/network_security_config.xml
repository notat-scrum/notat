<?xml version="1.0" encoding="utf-8"?>
<!--
  Os emuladores do Firebase falam HTTP puro, e o Android bloqueia texto claro
  desde o Android 9. A liberacao vale so para os hosts do emulador e so no
  source set de debug: o APK de release continua exigindo HTTPS em tudo.
  Para testar em aparelho fisico, acrescente aqui o IP da maquina na rede local.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
